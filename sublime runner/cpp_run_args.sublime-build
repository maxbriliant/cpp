{
    "cmd": [
        "bash", "-c",
        "g++ -std=c++23 '${file}' -o '${file_path}/${file_base}.out' && '~/scripts/sublime runner/run_with_args.sh' '${file}'"
    ],
    "file_regex": "^(..[^:]+):([0-9]+):?([0-9]+)?:? (.*)$",
    "working_dir": "${file_path}",
    "selector": "source.c++, source.c"
}
